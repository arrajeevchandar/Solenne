import 'package:flutter/material.dart';

class AppColors {
  static const midnight = Color(0xFF080D25);
  static const deepNavy = Color(0xFF112250);
  static const ink = Color(0xFF1A2F61);
  static const card = Color(0x663C507D);
  static const cardElevated = Color(0x993C507D);
  static const sapphire = Color(0xFF3C507D);
  static const royalBlue = Color(0xFF112250);
  static const quicksand = Color(0xFFE0C68F);
  static const swanWing = Color(0xFFF5F0E9);
  static const shellstone = Color(0xFFD8CBC2);
  static const aqua = Color(0xFF6DE6D5);
  static const electricBlue = Color(0xFF81A8FF);
  static const coral = Color(0xFFFF8F87);
  static const mint = Color(0xFF8BE0C1);
  static const violet = Color(0xFF9D7CFF);
  static const gold = quicksand;
  static const textPrimary = swanWing;
  static const textSecondary = shellstone;
  static const border = Color(0x663C507D);
  static const danger = Color(0xFFFF6B6B);

  // Backward-compatible aliases used by existing widgets while the UI migrates.
  static const creamBase = midnight;
  static const warmSand = cardElevated;
  static const softSage = mint;
  static const mutedTeal = aqua;
  static const pastelBlue = electricBlue;
  static const blushPink = coral;
  static const ivoryWhite = card;
  static const deepCharcoal = textPrimary;
  static const warmGrey = textSecondary;
  static const softLavender = violet;
}
